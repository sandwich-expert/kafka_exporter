#!/usr/bin/env bash

# Settings
REPOSITORY=thelastpickle/kafka-exporter
#This tag format matches the format generated by maven, otherwise it's 1 char 2 long
TAG=`git log -n 1 --pretty=format:"%h"`
TAG=${TAG:0:7}
IMAGE_NAME=$REPOSITORY:$TAG